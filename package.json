{
  "name": "child-raising-game",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "tsc -p tsconfig.json --noEmit false --outDir .tmp-test --rootDir src --module commonjs --moduleResolution node && node --test .tmp-test/core/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@typescript-eslint/parser": "^8",
    "eslint": "^9",
    "typescript": "^5.7.0"
  }
}
